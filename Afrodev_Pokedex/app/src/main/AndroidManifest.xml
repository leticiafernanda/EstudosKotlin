<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.afrodev_pokedex">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Afrodev_Pokedex">

        <activity
            android:screenOrientation="portrait"
            android:name=".PokemonEvolucao">

        </activity>
        <activity
            android:screenOrientation="portrait"
            android:name=".PokemonHabilidade" />
        <activity
            android:screenOrientation="portrait"
            android:name=".StatusPokemonActivity" />
        <activity
            android:screenOrientation="portrait"
            android:name=".PokemonActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashCarregarActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeActivity"
            tools:ignore="Instantiatable" />
    </application>

</manifest>